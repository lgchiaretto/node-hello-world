module.exports = {
  server: {
    port: 3000
  },
  app: {
    title: 'Example app'
  },
  lyra: {
    apiHost: 'chiaretto.com.br', // URL to Saga endpoint
    dataset: 'chiaretto'  // Public dataset (same as Venue dataset name suffixed with "--pub"
  }
}
